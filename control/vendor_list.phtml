<?php


if (isset($action) && $action == 'search') {

	$search_on = $_POST['search_value'];

	$main_query = "SELECT sku, name, description, price FROM products";

	$main_stmt = $db->query($main_query);


} else {

	$main_stmt = $db->query("SELECT * FROM vendors");

}



?>
          <div class="col-xs-12 col-md-9">
             
            <section id="page-header">

              <div id="searchCriteria" style="float:right;">
              <form name="searchForm" action="customers.php" method="post">
              <input type="hidden" name="action" value="search">
              <span class="inputLabel">SEARCH: <input type="text" name="search_value" value="" size="20">

              <button class="button" onClick="searchForm.submit();">GO!</button>

              </form>
              </div>
              <h1><span class="glyphicon glyphicon-shopping-cart"></span> Vendors</h1>
              </section>

            <section id="content">
                <div id="actionNavContainer">
                    <ul id="actionNav">
                    <li><a href="/control/vendors/create">Add New Vendor</a></li>
                    </ul>
                </div>
            

    	        <div style="padding-top:20px">
      	            <table class="detailsList">
      		        <tr><th>ID</th><th>NAME</th><th>PHONE</th><th>EMAIL</th><th>WEBSITE<th>USER</th></tr>

      	            <?php while ($result = $main_stmt->fetch_object()) : ?>
      		        <tr>
      		        <td><a href="/control/categories/edit?vendor_id=<?= $result->vendor_id ?>"><?= $result->vendor_id ?></a></td>
      		        <td><?= $result->name ?></td><td><?= $result->phone_tollfree ?></td><td><?= $result->email ?></td>
                    <td><a href="http://<?= $result->website ?>"><?= $result->website ?></a></td><td><?= $result->web_user ?></td>
      	            <?php endwhile ?>

      	            </table>
    	        </div>

            </section>

        </div><!-- /.col-sm-12 /.col-lg-9 -->